<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Student Dropout Analysis</title>

  <!-- Google Font & Icons -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;500;700&display=swap" rel="stylesheet"/>
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet"/>

  <style>
    :root {
      --clr-primary: #4a90e2;
      --clr-accent: #f5a623;
      --clr-dark: #2c3e50;
      --clr-light: #ecf0f1;
      --fw-light: 300;
      --fw-med: 500;
      --fw-bold: 700;
    }
    * {
      margin: 0; padding: 0;
      box-sizing: border-box;
      font-family: 'Poppins', sans-serif;
      scroll-behavior: smooth;
    }
    body {
      display: flex; flex-direction: column;
      min-height: 100vh;
      background: var(--clr-light);
      color: var(--clr-dark);
    }
    nav {
      background: var(--clr-dark);
      color: var(--clr-light);
      padding: 1rem 2rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    nav .logo {
      font-size: 1.5rem;
      font-weight: var(--fw-bold);
      letter-spacing: 2px;
    }
    nav ul {
      display: flex; gap: 1.5rem;
      list-style: none;
    }
    nav ul a {
      color: var(--clr-light);
      text-decoration: none;
      font-weight: var(--fw-med);
      transition: color .3s;
    }
    nav ul a:hover {
      color: var(--clr-accent);
    }

    .hero {
      flex: 1;
      background: linear-gradient(135deg, var(--clr-primary) 0%, var(--clr-dark) 100%);
      color: white;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
      padding: 4rem 1rem;
    }
    .hero h1 {
      font-size: 3rem;
      font-weight: var(--fw-bold);
      margin-bottom: 1rem;
      animation: fadeIn 1.2s ease-out;
    }
    .hero p {
      max-width: 600px;
      font-weight: var(--fw-light);
      margin-bottom: 2rem;
      line-height: 1.6;
      animation: fadeIn 1.5s ease-out 0.3s both;
    }
    .btn {
      display: inline-flex;
      align-items: center;
      padding: 0.75rem 1.75rem;
      background: var(--clr-accent);
      color: var(--clr-dark);
      font-weight: var(--fw-bold);
      text-transform: uppercase;
      border: none;
      border-radius: 50px;
      cursor: pointer;
      transition: transform .3s, box-shadow .3s;
      animation: popIn 1s ease-out 0.6s both;
    }
    .btn .material-icons {
      margin-left: 0.5rem;
      font-size: 1.2rem;
    }
    .btn:hover {
      transform: translateY(-3px);
      box-shadow: 0 8px 20px rgba(0,0,0,0.2);
    }

    footer {
      background: var(--clr-dark);
      color: var(--clr-light);
      text-align: center;
      padding: 1rem;
      font-size: 0.9rem;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(-20px); }
      to   { opacity: 1; transform: translateY(0); }
    }
    @keyframes popIn {
      from { opacity: 0; transform: scale(0.8); }
      to   { opacity: 1; transform: scale(1); }
    }
    @media (min-width: 768px) {
      .hero h1 { font-size: 4rem; }
      .hero p { font-size: 1.125rem; }
    }
  </style>
</head>
<body>

  <nav>
    <div class="logo">Dropout Analysis</div>
    <ul>
      <li><a href="/user/dashboard">Dashboard</a></li>
      <li><a href="/team">Team</a></li>
      <li><a href="/user/logout">Logout</a></li>
    </ul>
  </nav>

  <section class="hero">
    <h1>Hello, <%= user.charAt(0).toUpperCase() + user.slice(1) %>!</h1>
    <p>
      Welcome to the Student Dropout Analysis Dashboard. Answer a few questions to predict
      the likelihood of a student dropping out, so we can intervene early and help every
      learner succeed.
    </p>
    <button class="btn" onclick="location.href='/user/first'">
      Get Started <span class="material-icons">arrow_forward</span>
    </button>
  </section>

  <footer>
    &copy; 2025 Dropout Analysis • Built with care to support student success
  </footer>

</body>
</html>
